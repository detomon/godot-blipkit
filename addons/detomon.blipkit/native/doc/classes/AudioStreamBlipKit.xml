<?xml version="1.0" encoding="UTF-8" ?>
<class name="AudioStreamBlipKit" inherits="AudioStream" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:noNamespaceSchemaLocation="https://raw.githubusercontent.com/godotengine/godot/master/doc/class.xsd">
	<brief_description>
		Audio stream to playbackdata from a BlipKit context.
	</brief_description>
	<description>
		Allows to attach an arbitary number of [BlipKitTrack] objects.
		[codeblocks]
		[gdscript]
		# Keep this object around. Freed tracks are detached from the stream playback.
		var track := BlipKitTrack.new()

		# Some audio stream player with a [AudioStreamBlipKit] stream resource.
		var stream_player: AudioStreamPlayer = $AudioStreamPlayer

		# Make sure the stream is playing or has [member AudioStreamPlayer.autoplay] set before retreiving the playback object.
		stream_player.play()

		# Get the playback object.
		var playback: AudioStreamBlipKitPlayback = stream_player.get_stream_playback()

		# Attach the track.
		track.attach(playback)

		# Set some parameters and play a note.
		track.waveform = BlipKitTrack.WAVEFORM_TRIANGLE
		track.portamento = 8
		track.note = BlipKitTrack.NOTE_A_2
		[/gdscript]
		[/codeblocks]
	</description>
	<methods>
	</methods>
	<members>
		<member name="clock_rate" type="int" setter="" getter="" default="240">
			The number of ticks per second at which dividers and instrument sequences are updated.
		</member>
	</members>
</class>
