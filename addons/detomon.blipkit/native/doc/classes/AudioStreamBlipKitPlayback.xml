<?xml version="1.0" encoding="UTF-8" ?>
<class name="AudioStreamBlipKitPlayback" inherits="AudioStream" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:noNamespaceSchemaLocation="https://raw.githubusercontent.com/godotengine/godot/master/doc/class.xsd">
	<brief_description>
		Audio stream to playback data from a BlipKit context.
	</brief_description>
	<description>
		Allows to attach [BlipKitTrack] objects.
		[codeblocks]
		[gdscript]
		# Keep this object around. Freed tracks are detached from the stream playback.
		var track := BlipKitTrack.new()

		# Some audio stream player with a [AudioStreamBlipKit] stream resource.
		var stream_player: AudioStreamPlayer = $AudioStreamPlayer

		# Make sure the stream is playing or has [member AudioStreamPlayer.autoplay] set before retreiving the playback object.
		stream_player.play()

		# Get the playback object.
		var playback: AudioStreamBlipKitPlayback = stream_player.get_stream_playback()

		# Attach the track.
		track.attach(playback)

		# Set some parameters and play a note.
		track.waveform = BlipKitTrack.WAVEFORM_TRIANGLE
		track.portamento = 8
		track.note = BlipKitTrack.NOTE_A_2
		[/gdscript]
		[/codeblocks]
	</description>
	<methods>
		<method name="add_tick_function">
			<return type="void" />
			<param index="0" name="callable" type="Callable" />
			<param index="1" name="ticks" type="int" />
			<description>
				Adds a callback which is called every multiple number of ticks (see [member AudioStreamBlipKit.clock_rate]). The functions are called in the same order as they are added.
				[b]Note:[/b] The callback is called on the next tick after adding.
			</description>
		</method>
		<method name="clear_tick_functions">
			<return type="void" />
			<description>
				Remove all tick callbacks.
			</description>
		</method>
		<method name="get_tick_function_count">
			<return type="int" />
			<description>
				Returns the number of callbacks.
			</description>
		</method>
		<method name="remove_tick_function">
			<return type="void" />
			<param index="0" name="index" type="int" />
			<description>
				Removes a callback at [param index].
			</description>
		</method>
	</methods>
</class>
